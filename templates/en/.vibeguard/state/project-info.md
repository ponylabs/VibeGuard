# Project Info

Record stable project facts that future tasks will repeatedly use.

Do not record general rules, temporary process, or small details that can be cheaply checked in code.

## Tech Stack

No entries yet.

Template:

```text
- observed-in-code: Frontend uses React, see package.json.
- observed-in-code: Backend uses FastAPI, see pyproject.toml.
```

## Runtime And Environment

No entries yet.

Template:

```text
- observed-in-code: Python version is 3.12, see .python-version.
- user-approved: Local development must use uv for virtual environment management.
```

## Package And Tooling

No entries yet.

Template:

```text
- observed-in-code: Package manager is pnpm, see pnpm-lock.yaml.
- observed-in-code: TypeScript config entry is tsconfig.json.
```

## Key Directories

No entries yet.

Template:

```text
- observed-in-code: API routes live in src/routes/.
- observed-in-code: Shared UI components live in src/components/.
```

## Module Boundaries

No entries yet.

Template:

```text
- observed-in-code: billing module does not directly access auth database tables, see src/billing/README.md.
```

## Project Conventions

No entries yet.

Template:

```text
- user-approved: New pages should reuse existing layout components before adding parallel layout systems.
```
